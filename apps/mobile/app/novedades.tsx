import { NovedadesScreen } from '@/features/inicio/NovedadesScreen';

/** Ruta "Novedades" (§4.13): histórico de cambios normativos del paquete de contenido. */
export default function NovedadesRoute() {
  return <NovedadesScreen />;
}
