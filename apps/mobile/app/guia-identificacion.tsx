import { GuiaIdentificacionScreen } from '@/features/guia/GuiaIdentificacionScreen';

/**
 * Ruta de la GUÍA RÁPIDA DE IDENTIFICACIÓN Y CACHEO (`/guia-identificacion`). Se abre desde el acceso
 * rápido de seguridad ciudadana. Contenido de referencia bundlado y offline, sin datos de terceros.
 */
export default function GuiaIdentificacionRoute() {
  return <GuiaIdentificacionScreen />;
}
