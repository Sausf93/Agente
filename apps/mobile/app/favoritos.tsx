import { FavoritosScreen } from '@/features/inicio/FavoritosScreen';

/** Ruta "Tus favoritas" (§4.2/§4.4): la lista completa de infracciones marcadas como favoritas. */
export default function FavoritosRoute() {
  return <FavoritosScreen />;
}
