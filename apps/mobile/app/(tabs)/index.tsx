import { PlaceholderScreen } from '@/ui/PlaceholderScreen';

// Home = "Buscar" (ADR-003): el buscador es el núcleo del producto.
export default function BuscarScreen() {
  return (
    <PlaceholderScreen
      title="Buscar"
      hint="Aquí irá el buscador (FTS5 + jerga de calle + voz). Es la pantalla de arranque."
    />
  );
}
